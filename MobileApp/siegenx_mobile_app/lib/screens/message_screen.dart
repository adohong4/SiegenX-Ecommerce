import 'package:flutter/material.dart';

class MessageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(title: Text("Search")),
      body: Center(
        child: Text("NHẮN TIN", style: TextStyle(fontSize: 22)),
      ),
    );
  }
}
